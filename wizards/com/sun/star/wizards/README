To call a wizard remotely you need to:

-> For testing on core:
    export URE_BOOTSTRAP="file:///home/'USER'/freedomoffice-core/instdir/program/fundamentalrc"
    export PYTHONPATH=/home/'USER'/freedomoffice-core/instdir/program
    #and run FreedomOffice in listening mode:
    ./soffice --"accept=socket,host=localhost,port=2002;urp;"
      
-> For testing on installed FreedomOffice:
    export URE_BOOTSTRAP="file:///usr/lib/freedomoffice/program/fundamentalrc"
    export PYTHONPATH=/usr/lib/freedomoffice/program
    #and run FreedomOffice in listening mode:
    soffice --"accept=socket,host=localhost,port=2002;urp;"
      
-> Launch the wizard from wizards parent folder:
    python3.3
    from wizards.[Wizard's folder].CallWizard import CallWizard  
    CallWizard.callRemote()
